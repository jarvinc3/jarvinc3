import { motion } from 'framer-motion';

export const Experience = () => {
  return (
    <motion.div className="w-full relative h-screen p-[8rem] bg-transparent">
      <motion.h2 className="text-3xl font-bold text-[#032d3f]">Experience</motion.h2>
    </motion.div>
  )
}
