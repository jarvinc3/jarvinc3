import { motion } from 'framer-motion';

export const Work = () => {
  return (
    <motion.div className="w-full h-screen p-16 bg-transparent">
      <div className='w-full h-full bg-white rounded-xl'>
        work
    </div>
  </motion.div>
  )
}
